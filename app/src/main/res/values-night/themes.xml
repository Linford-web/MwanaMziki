<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EventMuziki" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/my_primary</item>
        <!-- Primary color -->
        <item name="colorPrimaryDark">@color/my_primary</item>
        <item name="colorAccent">@color/black</item>
        <!-- Customize your theme here -->
       <item name="android:windowBackground">@color/white</item>"
    </style>
    <!-- Customize your main theme here. -->
    <style name="Theme.EventMuziki" parent="Base.Theme.EventMuziki"/>

</resources>